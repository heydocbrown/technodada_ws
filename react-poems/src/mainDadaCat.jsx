import React from 'react'
import ReactDOM from 'react-dom/client'
import AppDadaCat from './AppDadaCat.jsx'

ReactDOM.createRoot(document.getElementById('react-root')).render(
  <React.StrictMode>
    <AppDadaCat />
  </React.StrictMode>,
)